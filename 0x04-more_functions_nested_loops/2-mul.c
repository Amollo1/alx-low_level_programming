#include "main.h"

/**
 * mul - Multipliers two integers
 * @x: The first integer
 * @y: The second integer
 * Return: The result of multiply the two integers passed
 */

int mul(int x, int y)
{
	return (x * y);
}
