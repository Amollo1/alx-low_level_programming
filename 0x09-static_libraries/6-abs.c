/**
 * _abs - computes computes absolute value of a number
 *
 * @x: number to find its absolute value
 *
 * Return: Absolute value of a number
 */

int _abs(int x)
{
	if (x < 0)
		x *= -1;
	return (x);
}
